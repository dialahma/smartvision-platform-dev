server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dialahma/smartvision-config-repo-dev.git
          username: ${CONFIG_REPO_GIT_USERNAME}
          password: ${CONFIG_REPO_GIT_PASSWORD}
          clone-on-start: true
          force-pull: true
          default-label: ${CONFIG_REPO_BRANCH:main}

management:
  endpoints:
    web:
      exposure:
        include: health,info,env,configprops,beans,prometheus
  endpoint:
    env:
      enabled: true
      
logging:
  level:
    org.springframework.cloud: DEBUG
    com.netflix: DEBUG
