spring:
  main:
    web-application-type: reactive
  cloud:
    config:
      enabled: false
eureka:
  client:
    enabled: false
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
